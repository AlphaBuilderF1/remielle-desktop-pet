# 隐私说明

生效日期：2026 年 8 月 22 日

版本：1.0

本说明适用于“蕾米埃尔 AI 桌面宠物”（以下简称“本软件”）。本软件是本地运行的非官方同人项目，项目维护者不运营聊天服务器。

## 简要结论

- 默认离线模式不发送网络请求。
- 本软件不包含遥测、广告、账户系统或自动更新检查，项目维护者不会通过本软件自动收集使用数据。
- 只有当你主动启用 AI 接口时，本软件才会把必要的对话上下文发送到你填写的第三方接口。

## 保存在本机的数据

便携版会在程序根目录保存以下文件；从源码运行时，它们保存在项目根目录：

- `config.json`：是否启用 AI、称呼、模型名、接口地址、性格设置和加密后的 API 密钥。
- `memory.json`：最近对话、用户明确要求记住的内容、情绪反馈和关系阶段数据。该文件是可读的普通 JSON，不会加密。
- `启动错误.log`：仅在启动发生未处理异常时生成，用于记录技术错误信息。

API 密钥通过 Windows DPAPI 与当前 Windows 登录账户绑定加密后写入 `config.json`。模型名、接口地址、称呼和其他设置不加密。你也可以不保存密钥，改用 `PET_API_KEY` 或 `OPENAI_API_KEY` 环境变量。

## 启用 AI 后会发送什么

启用 AI 后，本软件会直接请求你填写的兼容 `POST /v1/chat/completions` 接口。根据功能需要，请求可能包含：

- API 密钥和模型名。
- 你当前输入的消息及最近最多 12 条对话消息。
- 你设置的称呼、性格预设和自定义性格。
- 已启用记忆时，本地长期记忆、关系阶段和当前情绪反馈。
- 连接测试时的固定测试消息。
- 为生成主动待机台词而发送的当前本地时间和生成指令。该功能可能在后台补充台词缓存，因此会产生额外的第三方请求和费用。

请求会直接发送到你配置的接口地址，而不是经由项目维护者的服务器中转。第三方服务商还可能根据其自身规则获取 IP 地址、请求时间和其他技术信息。

## 第三方服务和费用

你选择的 AI 提供方是独立的第三方。其数据保留、训练使用、跨境传输、账号安全和计费方式适用该提供方的条款与隐私政策，不受本说明控制。

请只配置你信任的 `https://` 接口，不要通过未加密的 `http://` 地址发送 API 密钥或对话内容。在发送敏感信息前，请先阅读服务商的隐私政策，并确认你的账号权限、额度和费用。

## 数据保留、删除和备份

- 在“记忆与性格”中可以删除单条长期记忆，或清空长期记忆和最近对话。
- 要完全重置本软件，请先退出程序，再删除 `config.json`、`memory.json` 和 `启动错误.log`。
- 本软件不会自动上传或备份这些文件。如果文件位于网盘或系统备份目录，相关服务可能自行复制它们。
- 本地删除不会删除第三方 AI 提供方已经收到的数据；如需处理这些数据，请按该提供方的政策和工具操作。

## 安全建议

- 不要把 `config.json`、`memory.json` 或启动错误日志发给不信任的人。
- 不要在对话中输入密码、身份证件、金融账户、健康记录或其他高度敏感信息。
- 密钥丢失、电脑被入侵或误将配置文件公开时，请立即到对应 AI 服务商吊销密钥。

## 未成年人

本软件不主动判断用户年龄，也不面向未成年人收集个人信息。未成年人在启用第三方 AI 服务前，应获得监护人同意并遵守该服务的年龄要求。

## 更新与联系

本说明可能随功能变化而更新，更新后会在仓库中修改版本或生效日期。

如对本软件的本地数据处理有疑问，请通过本项目的 GitHub Issues 联系。请不要在公开 Issue 中附上 API 密钥、对话内容、配置文件或其他私密信息。
